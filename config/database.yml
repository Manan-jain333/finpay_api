default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: manan
  password:
  host: localhost

development:
  <<: *default
  database: finpay_api_development

test:
  <<: *default
  database: finpay_api_test

production:
  <<: *default
  database: finpay_api_production
